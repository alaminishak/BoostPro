workflows:
  android-app:
    name: Build BoostPro
    environment:
      java: 17

      android_signing: []   # <-- disables signing safely

    scripts:
      - name: Build Debug APK
        script: |
          ./gradlew assembleDebug || gradle assembleDebug

    artifacts:
      - app/build/outputs/**/*.apk
